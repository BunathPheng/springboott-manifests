---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springboott                  # Ingress name
  labels:
    app: springboott
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springboott.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springboott               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
